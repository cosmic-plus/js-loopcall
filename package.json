{
  "name": "@cosmic-plus/loopcall",
  "version": "1.0.5",
  "description": "A library that enable unlimited complex queries to Horizon nodes.",
  "author": "MisterTicot",
  "repository": "github:cosmic-plus/node-loopcall",
  "license": "MIT",
  "main": "loopcall.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write --no-semi '*.md' '*.json' '*.js' 'test/*.js'",
    "eslint": "eslint --fix '*.js' 'test/*.js'",
    "lint": "npm run prettier && npm run eslint"
  },
  "keywords": [
    "stellar",
    "horizon",
    "unlimited",
    "complex",
    "queries"
  ],
  "devDependencies": {
    "eslint": "^5.8.0",
    "prettier": "^1.17.0"
  },
  "dependencies": {
    "stellar-sdk": "^0.11.0"
  }
}
